{
  "name": "pastorpal-backend",
  "version": "1.0.0",
  "description":"Pastor Pal Laundry JSON Server backend",
  "main": "db.json",
  "scripts": {
    "start": "npx json-server --host 0.0.0.0 --watch db.json --port 10000"
  },

  "dependencies": {
    "json-server": "^0.17.4"
  }
}
